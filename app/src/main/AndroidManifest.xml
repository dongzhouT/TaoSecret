<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="secret.com.tao.taosecret">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".aty.AtyLogin"
            android:launchMode="singleTask" />
        <activity android:name=".aty.AtyTimeLine" />
        <activity android:name=".aty.AtyMessage" />
        <activity
            android:name=".aty.AtyPublish"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".aty.AtyTestListview" />
        <activity android:name=".aty.AtyMyContacts" />
        <activity
            android:name=".aty.AtyChangePwd"
            android:theme="@android:style/Theme.Dialog" />
        specifies
    </application>

</manifest>
